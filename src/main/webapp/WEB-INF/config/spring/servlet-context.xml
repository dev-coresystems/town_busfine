<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context
         http://www.springframework.org/schema/context/spring-context.xsd
         http://www.springframework.org/schema/mvc
         http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Annotation 기반 컨트롤러 활성화 -->
    <mvc:annotation-driven />

    <!-- Component Scan (Controller, Service 등) -->
    <context:component-scan base-package="com.busfine" />

    <!-- 정적 자원 처리 (js/css/images 등) -->
    <mvc:resources mapping="/resources/**" location="/resources/" />
    <mvc:resources mapping="/.well-known/**" location="/resources/.well-known/" /> <!-- google developer 자원 -->
    <!-- ViewResolver에 TilesView 적용 -->
    <bean class="org.springframework.web.servlet.view.tiles3.TilesViewResolver">
        <property name="order" value="1"/>
    </bean>

    <!-- View Resolver 설정 (JSP 기준) -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
        <property name="order" value="2"/>
    </bean>

    <!-- TilesConfigurer 설정 -->
    <bean id="tilesConfigurer" class="org.springframework.web.servlet.view.tiles3.TilesConfigurer">
        <property name="definitions">
            <list>
                <value>/WEB-INF/config/tiles/tiles.xml</value>
            </list>
        </property>
        <property name="checkRefresh" value="true" />
    </bean>

    <mvc:interceptors>
        <mvc:interceptor>
            <!-- 모든 경로에 적용 -->
            <mvc:mapping path="/**"/>
            <!-- 정적 리소스는 제외 -->
            <mvc:exclude-mapping path="/resources/**"/>
            <mvc:exclude-mapping path="/.well-known/**"/> <!--  google developer -->
            <mvc:exclude-mapping path="/error/handle"/> <!-- error page -->
            <bean class="com.busfine.home.common.handler.GbfInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>
</beans>